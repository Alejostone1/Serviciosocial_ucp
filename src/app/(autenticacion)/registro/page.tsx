import RegisterClient from './register-client';

export const metadata = {
    title: 'Registro de Estudiante | Servicio Social UCP',
};

export default function RegisterPage() {
    return <RegisterClient />;
}
